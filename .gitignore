dist
coverage
node_modules
.eslintcache
.env.local
.env

.ph
projects-import.js
.keypair.json

# Local-only artifacts
database-export-*.sql
tsconfig.tsbuildinfo
vault-fix-scripts/


# superpowers SDD scratch workspaces
.superpowers/

# Python bytecode caches (scripts/)
__pycache__/
*.pyc

# docling.rs models + pdfium (~707MB). Fetched once with
# `node scripts/docling-serve/fetch-models.mjs`, which defaults to these paths.
.models/
.pdfium/

# drive-sync run artifacts (resume markers, never commit)
scripts/drive-sync/data/*/id-map.json

.serena